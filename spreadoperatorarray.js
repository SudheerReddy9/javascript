// Declare a variable named greeting and assign the string "Hello" to it.
let greeting = "Hello";

// Spread the characters of the greeting string into an array and log the array.
console.log([...greeting]);
